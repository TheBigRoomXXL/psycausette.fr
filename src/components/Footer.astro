---
import { siteConfig } from "../site.config";
---

<footer>
  <div>
    Ce contenu est publié sous license
    <a href="https://creativecommons.org/licenses/by/4.0/" target="_blank">
      Creative Commons
    </a>
  </div>

  <nav>
    {
      siteConfig.socialLinks.map(({ name, url }) => (
        <a href={url} target="_blank">
          {name}
        </a>
      ))
    }
  </nav>
</footer>
<style>
  footer {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 1em;

    color: var(--footer-header-foreground) !important;
    background-color: var(--footer-header-background);
    box-shadow: var(--footer-header-shadow);
  }

  @media (min-width: 640px) {
    footer {
      flex-direction: row;
    }
  }

  a {
    color: var(--footer-header-link) !important;
  }
</style>
